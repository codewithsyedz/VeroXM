// Marketing route group: the cinematic, public-facing surface.
// Full art direction lives in docs/ART-DIRECTION.md — this layout is a
// Phase 0 placeholder until the visual concept is implemented.
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
