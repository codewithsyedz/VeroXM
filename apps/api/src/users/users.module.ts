import { Module } from '@nestjs/common';

// Phase 0 stub — mirrors the legacy UsersController. Providers/controllers
// land in Phase 1+ per the migration roadmap.
@Module({})
export class UsersModule {}
