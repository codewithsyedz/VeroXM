# Point at a staging copy of the prod DB first. Never run prisma migrate
# against production during Phase 0-4 of the strangler-fig migration.
DATABASE_URL="mysql://user:password@localhost:3306/mycms"
